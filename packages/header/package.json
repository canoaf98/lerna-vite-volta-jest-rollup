{
  "name": "header",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && rollup --config",
    "test": "jest --silent",
    "test:dev": "jest --verbose"
  },
  "dependencies": {
    "common": "*"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
